# Google Gemini API key (required for all AI features)
GEMINI_API_KEY=

# SMTP Configuration (only needed if you use email features)
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Admin Notification
ADMIN_EMAIL=

# Application
APP_URL=
